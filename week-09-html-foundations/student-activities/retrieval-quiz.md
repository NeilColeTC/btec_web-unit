# Week 9 Retrieval Quiz

Complete without notes where possible.

1. What is the main responsibility of HTML?
2. What is meant by semantic HTML?
3. What is the purpose of `<!DOCTYPE html>`?
4. What is stored inside the `<head>` rather than the visible `<body>`?
5. Why should each page have an accurate `<title>`?
6. What is the purpose of `<main>`?
7. What is the difference between `section` and `nav`?
8. Why should heading levels represent structure rather than visual size?
9. What is a relative path?
10. Give one example of a relative path to an image in an `images` subfolder.
11. What should useful alternative text communicate?
12. Why should a form control have a connected `<label>`?
13. What does the `required` attribute do?
14. Why does a working HTML form interface not necessarily mean data is being stored?
15. Why is Stage 1 deliberately presented without CSS or JavaScript?

## Challenge

Explain why a structurally meaningful HTML page can still be considered successful even if it looks visually plain in the browser.